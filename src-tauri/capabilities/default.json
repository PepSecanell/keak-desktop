{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, overlay and agents windows",
  "windows": ["main", "overlay", "agents"],
  "permissions": [
    "core:default",
    "opener:default",
    "deep-link:default"
  ]
}
